import styled from "styled-components";

const HiddenText = styled.span`
  border: 0;
  height: 1px;
  margin: -1px;
  overflow: hidden;
  padding: 0;
  position: absolute;
  whitespace: nowrap;
  width: 1px;
`;

export default HiddenText;
